============
Installation
============

At the command line::

    $ easy_install django-docutils

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-docutils
    $ pip install django-docutils
